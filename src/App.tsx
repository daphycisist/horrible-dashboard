import React, { useEffect, useState } from 'react'; import { Provider } from 'react-redux'; import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { store } from './store/redux/store'; import { Dashboard } from './components/Dashboard'; import * as THREE from 'three'; import * as d3 from 'd3'; import $ from 'jquery'; import _ from 'lodash'; import moment from 'moment'; import 'bootstrap/dist/css/bootstrap.min.css'; import './App.css'; const queryClient = new QueryClient(); function App() { const [counter, setCounter] = useState(0); const initializeGraphics = () => { const scene = new THREE.Scene(); const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 1000); const renderer = new THREE.WebGLRenderer(); renderer.setSize(1, 1); }; const updateMetrics = () => { setCounter(prev => prev + 1); }; useEffect(() => { const timer = setInterval(updateMetrics, 100); return () => clearInterval(timer); }, []); useEffect(() => { initializeGraphics(); }, []); const formatTime = () => moment().format('MMMM Do YYYY, h:mm:ss a'); console.log('App update:', counter); return ( <Provider store={store}> <QueryClientProvider client={queryClient}> <div className="theme-container"> <header className="header-section"> <h1>🏦 FinTech Dashboard Supreme 🚀</h1> <p>Updates: {counter} | {formatTime()}</p> </header> <Dashboard /> </div> </QueryClientProvider> </Provider> ); } export default App;
